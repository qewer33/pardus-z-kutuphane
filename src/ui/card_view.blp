using Gtk 4.0;
using Adw 1;

template $ZLibCardView: FlowBox {
  valign: start;
  max-children-per-line: 5;
  selection-mode: none;
  column-spacing: 12;
  row-spacing: 12;
  hexpand: true;
  vexpand: true;
}
