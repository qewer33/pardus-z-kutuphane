using Gtk 4.0;
using Adw 1;

template $ZLibCard: Adw.Bin {
  styles [
    "card",
    "acti",
  ]

  Box {
    orientation: vertical;
    spacing: 12;
    halign: center;
    valign: fill;
    vexpand: true;

    Overlay {
      Image card_icon_base {
        resource: "/tr/org/pardus/zkutuphane/resources/book.png";
        pixel-size: 200;
      }

      [overlay]
      Image card_icon_overlay {
        halign: center;
        valign: center;
        pixel-size: 72;
      }
    }

    Label card_title {
      label: _("Card Title");
      justify: center;
      wrap: true;
      wrap-mode: word_char;
      max-width-chars: 15;
      ellipsize: end;
      valign: end;
      margin-top: 12;
      margin-start: 12;
      margin-end: 12;

      styles [
        "heading",
        "body",
      ]
    }

    Label card_publisher {
      justify: center;
      wrap: true;
      wrap-mode: word_char;
      max-width-chars: 15;
      ellipsize: end;
      valign: start;
      sensitive: false;
      margin-bottom: 12;
      margin-start: 12;
      margin-end: 12;

      styles [
        "caption",
        "dim-label",
      ]
    }
  }
}
